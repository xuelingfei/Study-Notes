定义：
    number10 = 2575  # 十进制
    number16 = 0xA0F  # 十六进制
    number8 = 0o37  # 八进制

格式化（结果为str）：
    %d - 格式化整数
    %u - 格式化无符号整型
    %o - 格式化无符号八进制数
    %x - 格式化无符号十六进制数
    %X - 格式化无符号十六进制数（大写）