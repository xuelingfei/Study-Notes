Python打包为exe文件所使用的组件是pyinstaller，安装方法：
1.使用pip install pyinstaller进行网络安装；
2.在网上下载压缩包版本，之后解压进行安装，
使用pip命令安装的话，会将该组件需要的其他组件一起安装，使用压缩包安装方式会存在依赖组件没有安装的情况。
安装成功之后，进入要压缩的文件目录，执行pyinstaller -F file，其中file是需要压缩的文件，之后就可以在该目录下新生成的dist文件夹中找到打包完成后的exe文件。